import React from "react";
import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles(theme => ({}));

const BeverageList = () => {
  const classes = useStyles();
  return <h1 id="bugerList">음료수 보여 줄거야~</h1>;
};

export default BeverageList;
