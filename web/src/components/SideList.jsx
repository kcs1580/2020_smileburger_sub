import React from "react";
import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles(theme => ({}));

const SideList = () => {
  const classes = useStyles();
  return <h1 id="bugerList">사이드메뉴 보여 줄거야~</h1>;
};

export default SideList;
